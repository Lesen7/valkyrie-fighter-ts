/**
* A slight addition to the Math.random function. Useful for generating random percentages.
* @return returns a random number between 0.1 and 1.0.
*/
export function randomPercent() {
    return Math.random() + 0.1;
}